
import { makeStyles} from "@material-ui/core/styles";

export let  useStyles = makeStyles((theme) => ({
    quotetext: {
      position: "absolute",
      zIndex: 1000,
      top: "250px",
      left: "30%",
      color: "#111940",
      fontSize: "2rem",
    },
  }));